import styled from "styled-components";

export const ListContaier = styled.div`
  list-style: none;
  display: flex;
  align-items: center;
  width: 1440px;
  margin: 0rem auto;
  border: 0.1rem solid rgba(0, 0, 0, 0.233);
  padding: 1rem;
  justify-content: space-between;

  p {
    max-width: 100px;
  }
`
